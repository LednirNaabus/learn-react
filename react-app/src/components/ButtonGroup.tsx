import { Fragment, ReactNode } from "react";

interface ButtonGroupProp {
  //   children: ReactNode;
  color?: string;
}

const ButtonGroup = ({ color }: ButtonGroupProp) => {
  let colors = ["primary", "warning", "success", "danger"];
  return <Fragment></Fragment>;
};

export default ButtonGroup;
